import { StationIconDirective } from './station-icon.directive';

describe('StationIconDirective', () => {
  it('should create an instance', () => {
    const directive = new StationIconDirective();
    expect(directive).toBeTruthy();
  });
});
